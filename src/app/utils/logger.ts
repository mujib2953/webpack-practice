export const log = (msg: any) => {
    console.log(msg);
}

export const logError = (error: any) => {
    console.error(error);
}

export const logWarning = (warnings: any) => {
    console.warn(warnings);
}
